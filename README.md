[Download APK](https://github.com/torokattila/WalletAndroidApp/blob/master/app-universal-release.apk)

## Building the Android APK

To build a release APK, run the following command from the `app/` directory:

```bash
npm run build:android-apk
```

After the build completes, the release APK can be found at:

```
app/android/app/build/outputs/apk/release/app-universal-release.apk
```